# Core dashboard (M4)
streamlit>=1.33
plotly>=5.20
pandas>=2.0
streamlit-autorefresh>=1.0.1

# Optional backend (matches proposal stack) - not required for the basic demo
fastapi>=0.110
uvicorn>=0.29

# Data layer (M3) - MQTT transport. Optional: only needed with USE_MQTT=1
paho-mqtt>=2.0

# AI assistant (M5) - supports BOTH OpenAI and Anthropic Claude. The provider is
# auto-detected from the API key prefix (sk-proj-… -> OpenAI, sk-ant-… -> Claude).
# Optional: a rule-based fallback is used automatically when no key is set.
# anthropic>=0.40 avoids the older anthropic/httpx 'proxies' TypeError that left
# the client stuck on "Rule-based" even with a valid key.
openai>=1.30
anthropic>=0.40

# Loads ANTHROPIC_API_KEY / USE_MQTT from a local .env file
python-dotenv>=1.0
